import { NextResponse } from "next/server";
import { requireRole } from "@/lib/auth";
export async function GET(){try{const {supabase}=await requireRole(["admin"]);const [reports,active,profiles,queue,rules,logs]=await Promise.all([supabase.from("flood_reports").select("*",{count:"exact",head:true}),supabase.from("sos_alerts").select("*",{count:"exact",head:true}).not("status","in",'("resolved","cancelled")'),supabase.from("profiles").select("id,full_name,role,created_at",{count:"exact"}).order("created_at",{ascending:false}).limit(20),supabase.from("flood_reports").select("id,location_name,depth,verification_status,created_at").order("created_at",{ascending:false}).limit(20),supabase.from("passability_rules").select("id,caution_depth,max_depth,active,vehicle_types(name,slug)").order("created_at"),supabase.from("audit_logs").select("id,action,entity_type,created_at,metadata").order("created_at",{ascending:false}).limit(20)]);for(const r of [reports,active,profiles,queue,rules,logs])if(r.error)throw r.error;return NextResponse.json({counts:{reports:reports.count,active:active.count,users:profiles.count},profiles:profiles.data,reports:queue.data,rules:rules.data,logs:logs.data});}catch(e){const m=e instanceof Error?e.message:"Unable to load admin data";return NextResponse.json({error:m},{status:m==="FORBIDDEN"?403:500});}}
