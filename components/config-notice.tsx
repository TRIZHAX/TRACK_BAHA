import { TriangleAlert } from "lucide-react";
export function ConfigNotice() { if (process.env.NEXT_PUBLIC_SUPABASE_URL && process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY) return null; return <div className="mb-5 flex items-start gap-3 rounded-2xl border border-amber-300 bg-amber-50 p-4 text-sm text-amber-950 dark:border-amber-800 dark:bg-amber-950 dark:text-amber-100"><TriangleAlert className="mt-0.5 shrink-0" size={18}/><div><strong>Backend setup required.</strong> Add the Supabase environment variables and apply the included migration. Operational actions remain disabled rather than simulating success.</div></div>; }
